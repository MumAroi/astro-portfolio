---
import { getCollection } from "astro:content";
import ProjectTitle from "../ui/ProjectTitle.astro";
import Heading from "../ui/Heading.astro";
import Link from "../ui/Link.astro";
const projects = await getCollection("projects");
---

<section aria-lable="Recent Projects">
    <div class="projects">
        <div class="rows">
            <Heading tagName="h2" size="h2">
                Don’t take our word for it
            </Heading>
            <p>
                Here are some recent websites and apps you ans explore for
                youself!
            </p>
            <Link href="projects" variant="secondary">
                See all projects
            </Link>
        </div>
        <div class="grid-wrapper">
            {
                projects
                    .slice(0, 3)
                    .map(({ data }) => <ProjectTitle data={data} />)
            }
        </div>
    </div>
</section>
